# 'pubspec.yaml' manages the assets and dependencies

name: flutter_tutorial
description: built redux example

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.0
  flutter:
    sdk: flutter

dev_dependencies:
  redux: ^4.0.0+3
  flutter_redux: ^0.6.0
  redux_thunk: ^0.3.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
